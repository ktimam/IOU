[package]
name = "iou_backend"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
candid = "0.10"
ic-cdk = "0.20"
ic-cdk-macros = "0.20"
ic-cdk-management-canister = "0.1"
serde = { version = "1", features = ["derive"] }

[features]
default = []

[profile.release]
opt-level = "z"
lto = true
