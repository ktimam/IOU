node_modules/
dist/
.DS_Store
.env
.env.local
.env.*.local

# dfx
.dfx/
.wallet/
dfx.json.bak
*.mo.bak

# local Internet Identity build (downloaded by scripts/dev-ii.sh)
ii/

# editor
.vscode/
.idea/

# build artifacts
*.wasm
target/

# Android release signing (v1.1.5) — NEVER commit keystores or secrets
android/keystore.properties
*.jks
*.keystore

# OpenChat stand-in test keys (local full-cycle check) — never commit
scripts/iou-relay/.standin-*.pem

# Persistent OpenChat registrar identity (scripts/register-openchat-app.ts) — it OWNS the
# registered "iou" app (upsert key = owner + name), so it is a secret; never commit
.openchat-registrar.json

# TypeScript / Vite build output
*.tsbuildinfo
vite.config.d.ts
vite.config.js

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# tests
coverage/
playwright-report/
test-results/

# OS
Thumbs.db

# Local Claude Code MCP registration — machine-specific (absolute paths + wsl.exe).
# Portable setup instructions live in scripts/iou-mcp/README.md.
.mcp.json
